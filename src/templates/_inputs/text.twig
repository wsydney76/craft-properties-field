{% import '_includes/forms.twig' as forms %}

{{ forms.text({
    name: propertyConfig.handle,
    type: propertyConfig.type,
    value: value ?? ''
}|merge(fieldConfig)) }}

{% if fieldConfig and 'suffix' in fieldConfig %}
    {{ fieldConfig.suffix }}
{% endif %}