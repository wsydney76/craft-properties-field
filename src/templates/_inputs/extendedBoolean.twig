{% import '_includes/forms.twig' as forms %}

<div style="padding: 2px 8px 2px 8px; display: flex; align-items: center">
    {{ forms.lightswitch({
        name: "#{propertyConfig.handle}[isOn]",
        on: value['isOn'] ?? '',
    }) }}

    {{ forms.text({
        name: "#{propertyConfig.handle}[comment]",
        label: 'Comment',
        class: 'text-margin',
        placeholder: 'Comment'|t('_properties-field'),
        value: value['comment'] ?? ''
    }) }}
</div>